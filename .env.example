PORT=3000
NODE_ENV=development

# Database — PostgreSQL via Docker (drizzle-orm + pg)
DATABASE_URL=postgresql://flyrank:flyrank123@localhost:5432/flyrank_widgets?schema=public

DB_HOST=localhost
DB_PORT=5432
DB_USER=flyrank
DB_PASSWORD=flyrank123
DB_NAME=flyrank_widgets

# JWT Auth Secret
JWT_SECRET=super_secret_jwt_key_change_in_production
JWT_EXPIRES_IN=7d

# Base URLs
API_BASE_URL=http://localhost:3000
TEST_SITE_ORIGIN=http://localhost:5500
